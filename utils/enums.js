const PageEnum = {
  // Should be improve: 
  // It's usually better to maintain one letter case in enum naming 
  SiteWrapper: {
    SEARCH: document.querySelector(`.columns__search-item`),
    rating: document.querySelector(`.columns__rating`)
  }
};
